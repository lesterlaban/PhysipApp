<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PhysipApp.Views.ConsultaDetailPage"
    xmlns:model="clr-namespace:PhysipApp.Models"  
    Title="{Binding Title}">

    <ContentPage.Content>
        <CollectionView x:Name="ItemsListView" ItemsSource="{Binding Items}" SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Spacing="20" x:DataType="model:Recurso">
                    <Label Text="{Binding Titulo}" FontSize="Medium"/>
                    <Label Text="{Binding Descripcion}" FontSize="Small"/>
                    <WebView HeightRequest="800"  HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"  Source="{Binding Url}"/>
                </StackLayout>

            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    </ContentPage.Content>
</ContentPage>